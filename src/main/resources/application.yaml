spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/edux"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    date-format: dd-MM-yyyy
server:
  port: 8080
  servlet:
    context-path: /edux
jwt:
  signerKey: "syyHIBUt5rezeijw9MmsyrWrweJLzOFc"
